import Extension from './Extension';

export default class CleanFormat extends Extension {
    get name() {
        return 'clean_format';
    }
    
    
    commands() {
        return () => function(state, dispatch, view) {
        
        }
   
      
    }
    
}
